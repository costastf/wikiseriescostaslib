============
Installation
============

At the command line::

    $ pip install wikiseriescostaslib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wikiseriescostaslib
    $ pip install wikiseriescostaslib

Or, if you are using pipenv::

    $ pipenv install wikiseriescostaslib

